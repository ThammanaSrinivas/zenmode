package com.zenlauncher.zenmode

import androidx.fragment.app.Fragment

class ClockWidgetFragment : Fragment(R.layout.fragment_widget_clock) {
    // TextClock handles its own updates, so no extra logic is needed here.
}
